# Svelte 5 Runes 迁移完整指南

> 自动化转换规则、深度解析和快速参考 - 一站式 Svelte 5 迁移解决方案

## 📚 文档结构

我为你创建了一套完整的 Svelte 5 迁移文档系统，包含三个核心文档：

### 1. 📘 完整迁移规则

**文件**: `.cursor/rules/svelte5-runes-migration.mdc`

**用途**: Cursor AI 自动识别和转换规则

**内容**:

- 14 个自动转换规则
- 详细的 before/after 示例
- 完整的迁移检查清单
- 常见错误和解决方案
- 完整的迁移示例

**何时使用**:

- 让 Cursor AI 参考以自动转换代码
- 作为完整迁移手册查阅

### 2. 🔍 深度问题解析

**文件**: `.cursor/rules/svelte5-migration-explained.md`

**用途**: 理解为什么迁移会失败以及如何成功

**内容**:

- 7 个主要失败原因详解
- 5 个成功关键因素
- 3 个实际案例分析
- 从失败到成功的完整路径
- 核心概念映射表

**何时使用**:

- 遇到迁移困难时理解根本原因
- 学习 Svelte 5 响应式系统变化
- 理解为什么某些转换必须这样做

### 3. ⚡ 快速参考速查表

**文件**: `.cursor/rules/svelte5-quick-reference.md`

**用途**: 快速查找转换语法

**内容**:

- 一页纸转换对照表
- 一分钟诊断检测点
- 三步转换法
- TOP 5 常见错误
- 复制粘贴代码模板
- 极速转换流程

**何时使用**:

- 快速查找某个语法如何转换
- 作为备忘单随时查阅
- 批量转换时的检查清单

### 4. 🌊 Windsurf AI 规则

**文件**: `.windsurf/rules/svelte5-auto-convert.md`

**用途**: Windsurf AI 自动识别和转换规则（与 Cursor 规则类似）

**内容**:

- 自动检测触发器
- 10 个核心转换规则
- 关键错误避免策略
- 完整转换检查清单
- AI 辅助提示

**何时使用**:

- 使用 Windsurf AI 时的自动转换参考

---

## 🚀 快速开始

### 方式 1: 使用自动化工具（推荐）

```bash
# 安装 Svelte 官方迁移工具
npx sv migrate svelte-5

# 这会自动完成 80% 的转换工作
```

### 方式 2: 使用 AI 辅助（Cursor/Windsurf）

1. **打开需要迁移的 `.svelte` 文件**
2. **呼叫 AI**: "请帮我将这个组件转换为 Svelte 5 Runes 模式"
3. **AI 会参考规则文档自动转换**
4. **检查和测试转换结果**

### 方式 3: 手动转换

1. **打开** `.cursor/rules/svelte5-quick-reference.md`
2. **使用"一分钟诊断"检测需要转换的部分**
3. **参考"快速转换表"逐项转换**
4. **使用"测试检查清单"验证**

---

## 📋 转换检查清单

在迁移每个组件时，使用这个清单确保完整转换：

### 阶段 1: 语法转换

```
[ ] export let → let { } = $props()
[ ] let → let = $state()
[ ] $: derived → const = $derived()
[ ] $: { effect } → $effect(() => {})
[ ] on:event → onevent
[ ] createEventDispatcher → callback props
[ ] <slot> → {@render}
[ ] const + bind:this → let + bind:this
```

### 阶段 2: 编译验证

```
[ ] 无编译错误
[ ] 无 TypeScript 类型错误
[ ] 无未使用的导入
[ ] 无混合语法警告
```

### 阶段 3: 功能测试

```
[ ] 组件正常渲染
[ ] Props 传递正常
[ ] 事件触发正常
[ ] 响应式更新正常
[ ] 插槽/Snippets 正常
[ ] 双向绑定正常
```

---

## 🎯 核心转换模式

### 最常见的 5 个转换：

#### 1. Props 声明

```svelte
// Before
export let title;
export let count = 0;

// After
let { title, count = 0 } = $props();
```

#### 2. 响应式状态

```svelte
// Before
let items = [];

// After
let items = $state([]);
```

#### 3. 派生值

```svelte
// Before
$: total = items.reduce((a, b) => a + b, 0);

// After
const total = $derived(items.reduce((a, b) => a + b, 0));
```

#### 4. 事件处理

```svelte
<!-- Before -->
<button on:click={handler}>Click</button>

<!-- After -->
<button onclick={handler}>Click</button>
```

#### 5. 默认插槽

```svelte
<!-- Before (子组件) -->
<div class="container">
  <slot />
</div>

<!-- After (子组件) -->
<script>
  let { children } = $props();
</script>
<div class="container">
  {@render children?.()}
</div>
```

---

## 💡 为什么迁移会失败？

### 主要原因总结：

1. **部分转换**（混合新旧语法）❌
   - 一旦使用任何 rune，整个组件必须完全转换
2. **const 用于 bind:this** ❌

   - 必须使用 `let` 声明

3. **混淆 $derived 和 $effect** ❌

   - 计算值用 `$derived`
   - 副作用用 `$effect`

4. **忘记转换父组件** ❌

   - 子组件改了 slots，父组件也要改

5. **事件处理器签名错误** ❌
   - 不再有 `e.detail`，直接是数据

### 成功的关键：

✅ **系统性转换**（一次性完整转换）  
✅ **正确的转换顺序**（Props → 状态 → 派生 → 副作用 → 事件 → 插槽）  
✅ **理解概念变化**（编译时 → 运行时响应式）  
✅ **使用工具辅助**（迁移脚本 + AI + 手动）  
✅ **测试驱动验证**（每步都测试）

---

## 🔥 常见错误速查

### 错误 1: "Cannot bind to a non-bindable property"

```svelte
// ❌ 错误
const ref = $state();

// ✅ 修复
let ref = $state();
```

### 错误 2: "Props are not bindable by default"

```svelte
// ❌ 子组件错误
let { value } = $props();

// ✅ 修复
let { value = $bindable() } = $props();
```

### 错误 3: "children is not defined"

```svelte
// ❌ 错误
{@render children?.()}

// ✅ 修复
<script>
  let { children } = $props();
</script>
{@render children?.()}
```

### 错误 4: "Cannot use $state in non-runes mode"

```svelte
// ❌ 混合模式
export let old;
let modern = $state();

// ✅ 全部 runes
let { old } = $props();
let modern = $state();
```

---

## 📖 学习路径

### 初学者路径：

1. **阅读**: 快速参考速查表（了解语法对照）
2. **实践**: 转换一个简单组件
3. **理解**: 深度问题解析（理解为什么）
4. **掌握**: 使用完整迁移规则作为手册

### 进阶路径：

1. **阅读**: 深度问题解析（理解响应式系统变化）
2. **参考**: 完整迁移规则（深入每个细节）
3. **实践**: 转换复杂组件（带 slots、events 等）
4. **优化**: 根据最佳实践优化代码

### 专家路径：

1. **自动化**: 配置 AI 使用规则文档
2. **批量**: 使用迁移脚本 + 手动优化
3. **验证**: 建立测试流程确保质量
4. **分享**: 帮助团队理解和应用

---

## 🛠️ 实用命令集合

### 检测需要迁移的文件

```bash
# 查找包含 export let 或 $: 的文件
grep -r "export let\|\$:" src/ --include="*.svelte" | \
  grep -v "\$state\|\$props\|\$derived\|\$effect"
```

### 查找特定问题

```bash
# 查找 const + bind:this 问题
grep -r "const.*=.*\$state()" src/ -A 5 -B 5 | grep "bind:this"

# 查找未转换的 on: 指令
grep -r "on:" src/ --include="*.svelte"

# 查找未转换的 <slot>
grep -r "<slot" src/ --include="*.svelte"
```

### 批量操作

```bash
# 整个项目迁移（自动）
bunx sv migrate svelte-5

# 干跑模式（只检查不修改）
bunx sv migrate svelte-5 --dry-run

# 更新依赖
bun update svelte@latest
```

---

## 📊 转换优先级

### 🔴 立即转换（高优先级）:

- 包含 `export let` 的组件
- 包含 `$:` 响应式声明的组件
- 使用 `createEventDispatcher` 的组件

### 🟡 优先转换（中优先级）:

- 使用 slots 的组件
- 使用生命周期钩子的组件
- 复杂交互组件

### 🟢 按需转换（低优先级）:

- 纯展示组件
- 静态组件
- 工具函数文件

---

## 🎓 最佳实践

### 转换前：

1. ✅ **备份代码**（`git commit`）
2. ✅ **更新依赖**（`bun update svelte@latest`）
3. ✅ **阅读文档**（本指南）
4. ✅ **准备测试**（确保有测试覆盖）

### 转换中：

1. ✅ **一次一个组件**（避免大范围破坏）
2. ✅ **遵循检查清单**（确保完整转换）
3. ✅ **边转换边测试**（及时发现问题）
4. ✅ **记录特殊情况**（复杂逻辑单独处理）

### 转换后：

1. ✅ **运行所有测试**（确保功能正常）
2. ✅ **检查性能**（Svelte 5 应该更快）
3. ✅ **更新文档**（记录变化）
4. ✅ **团队分享**（让其他人了解）

---

## 🆘 获取帮助

### 遇到问题时的步骤：

1. **查看错误信息**  
   Svelte 5 的错误提示非常清晰，通常会告诉你如何修复

2. **检查本文档**

   - 快速查找 → 快速参考速查表
   - 理解原因 → 深度问题解析
   - 完整指导 → 完整迁移规则

3. **使用 AI 助手**

   - Cursor AI 会自动参考规则文档
   - Windsurf AI 也配置了相应规则

4. **查阅官方文档**

   - [Svelte 5 迁移指南](https://svelte.dev/docs/svelte/v5-migration-guide)
   - [Runes 文档](https://svelte.dev/docs/svelte/runes)

5. **社区求助**
   - [Svelte Discord](https://svelte.dev/chat)
   - [GitHub Discussions](https://github.com/sveltejs/svelte/discussions)

---

## 📈 进度追踪

### 建议创建迁移追踪表：

```markdown
# Svelte 5 迁移进度

## 已完成 ✅

- [ ] components/Button.svelte
- [ ] components/Card.svelte
- [ ] ...

## 进行中 🔄

- [ ] components/Modal.svelte (遇到复杂 slots)
- [ ] ...

## 待处理 📋

- [ ] components/Layout.svelte
- [ ] ...

## 遇到的问题 🐛

- Modal.svelte: 需要处理嵌套 snippets
- ...
```

---

## 🎉 迁移完成后

### 享受 Svelte 5 的好处：

1. **更好的性能** 🚀

   - 运行时响应式更高效
   - 更小的包体积
   - 更快的渲染速度

2. **更清晰的代码** 📝

   - 显式的响应式声明
   - 更好的可读性
   - 更容易理解和维护

3. **更强的类型安全** 🛡️

   - 更好的 TypeScript 支持
   - 更准确的类型推断
   - 更少的运行时错误

4. **更优的开发体验** 💻
   - 更好的 IDE 支持
   - 更清晰的错误提示
   - 更容易调试

---

## 📚 相关资源

### 官方资源

- [Svelte 5 官方文档](https://svelte.dev/docs/svelte)
- [Runes 介绍博客](https://svelte.dev/blog/runes)
- [迁移脚本源码](https://github.com/sveltejs/svelte/tree/main/packages/migrate)

### 社区资源

- [Svelte Society](https://sveltesociety.dev/)
- [Svelte 中文社区](https://svelte.nodejs.cn/)

---

## 💬 反馈与改进

如果你发现：

- 文档中的错误
- 缺少的场景
- 可以改进的地方

请随时更新这些文档或向团队反馈！

---

## 📝 总结

你现在拥有：

1. ✅ **完整的迁移规则**（Cursor + Windsurf AI 参考）
2. ✅ **深度问题解析**（理解为什么失败和如何成功）
3. ✅ **快速参考速查表**（随时查找转换语法）
4. ✅ **这个总览文档**（整合所有资源）

**开始迁移吧！** 🚀

记住核心原则：

- 🎯 **完整转换**（不要混合语法）
- 🔍 **理解变化**（不只是替换语法）
- 🧪 **测试验证**（确保功能正常）
- 📚 **参考文档**（遇到问题就查）
